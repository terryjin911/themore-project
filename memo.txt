<2022-06-15> 
    화면이 "어떻게 굴러갔으면 좋겠는가"에 대한 생각을 했음
    우연히 전에 html실습했던 자료중에 "할인가격 구하기"를 찾았음(경로:자바스크립트DAY2-practice2.html)

        [           ]  1.여기에다가 값을 다 "때려넣고"
        [추가하기]     2.이걸 "누를때마다" 배열(Array)에 "차곡차곡 쌓임"
        입력받은 값:   3.그 배열(Array)를 입력받을때마다 여기서 화면에 "쏴주고"(정렬되지않은상태)
        [돌려보자]     4.이걸 누르면 "결과"에서 최대값/최소값이
        결과:            내림차순/오름차순으로 "정렬"해서 "쏴주고"


    index.html 문서 => (자바스크립트DAY2-practice2.html)와 대입해서 보기 
        -price => oPrice            영수증 가격
        -add                        누르면 price에서 입력한 값이 showAdd로 들어가서 저장됨
        -showAdd => showResult      입력한 영수증 가격을 화면에 뿌려줌

    오후9:40 : "여기까지실행-2"까지 정상 실행되고,
                undefine에러 다 잡았음=>(이건 list를 없앴음)
                다만, input에서 값을 받아와서 [추가하기]버튼을 누르면 먼가 작동이 될줄알았는데
                그냥 버튼만 누르는 사람됐음

<2022-06-16>
    오전9:00 : git올릴까.. 무거운 프레임워크 안쓸 예정이라 노트북 터지진않을거같은데












